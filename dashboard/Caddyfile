:80 {
    root * /usr/share/caddy
    encode gzip

    # SPA routing — rutas desconocidas vuelven a index.html
    try_files {path} /index.html
    file_server

    header {
        -Server
        X-Frame-Options DENY
        X-Content-Type-Options nosniff
    }
}
